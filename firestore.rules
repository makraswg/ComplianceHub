rules_version = '2';

// Deployment trigger: 2026-02-02T23:00:00Z - Addressing ID: ca9 and stabilizing root-level access

service cloud.firestore {
  match /databases/{database}/documents {
    
    // helper functions
    function isSignedIn() {
      return request.auth != null;
    }
    
    function isTenantMember(tenantId) {
      return isSignedIn() && exists(/databases/$(database)/documents/tenants/$(tenantId)/members/$(request.auth.uid));
    }

    // ROOT LEVEL RULES
    // These allow the queries used in the dashboard (e.g., collection(db, 'users'))
    match /users/{userId} {
      allow read, write: if isSignedIn();
    }
    match /resources/{resourceId} {
      allow read, write: if isSignedIn();
    }
    match /entitlements/{entitlementId} {
      allow read, write: if isSignedIn();
    }
    match /assignments/{assignmentId} {
      allow read, write: if isSignedIn();
    }
    match /auditEvents/{eventId} {
      allow read, write: if isSignedIn();
    }
    match /tenants/{tenantId} {
      allow read, write: if isSignedIn();
    }

    // COLLECTION GROUP RULES
    // Allows users to find which tenants they belong to
    match /{path=**}/members/{uid} {
      allow list, get: if isSignedIn() && (request.auth.uid == uid);
    }

    // NESTED TENANT RULES (Isolation)
    match /tenants/{tenantId} {
      match /members/{uid} {
        allow read, write: if isSignedIn();
      }
      match /users/{userId} {
        allow read, write: if isSignedIn();
      }
      match /resources/{resourceId} {
        allow read, write: if isSignedIn();
      }
      match /entitlements/{entitlementId} {
        allow read, write: if isSignedIn();
      }
      match /assignments/{assignmentId} {
        allow read, write: if isSignedIn();
      }
      match /auditEvents/{eventId} {
        allow read, write: if isSignedIn();
      }
    }

    // PLATFORM RULES
    match /platformUsers/{uid} {
      allow read: if isSignedIn() && request.auth.uid == uid;
    }
    
    // GLOBAL DEFAULT
    match /{document=**} {
      allow read, write: if false;
    }
  }
}